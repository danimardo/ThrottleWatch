{
  "name": "throttlewatch-design-system-harness",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Reproducible verification harness for the ThrottleWatch Svelte design system (../). Not part of the app you're building — this is only how to re-check this package after you change it, or browse every example composition. Copy the package's files (everything one level up except this folder) into your own app; don't copy this harness into it.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json --fail-on-warnings && tsc -p tsconfig.node.json",
    "test": "vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/svelte": "5.4.2",
    "@tsconfig/svelte": "5.0.8",
    "@types/node": "24.13.3",
    "jsdom": "30.1.0",
    "svelte": "5.57.0",
    "svelte-check": "4.7.6",
    "typescript": "6.0.3",
    "vite": "8.3.0",
    "vitest": "5.0.1"
  }
}
